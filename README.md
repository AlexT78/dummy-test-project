# Dummy Test Project

A minimal Python project to test Hermes Agent's GitHub PR workflow.

## Usage

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main.py
```
